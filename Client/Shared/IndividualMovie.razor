
<div style="background-color: @(Movie.KeyID % 2 == 0 ? "blue" : "red")">
    <p>Title: @((MarkupString)Movie.Title)</p>
    <p>Release Date: <b>@Movie.ReleaseDate.ToString("dd MMM yyyy")</b></p>
    @if (DisplayButtons)
    {
        <div>
            <button @onclick="Delete">Delete</button>
        </div>
    }

</div>



@code {
    [Parameter] public Movie Movie { get; set; }
    [Parameter] public bool DisplayButtons { get; set; } = false;

    void Delete()
    {
        Console.WriteLine($"Deleting {Movie.Title}");
    }
}
